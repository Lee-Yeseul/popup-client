export default function MyPage() {
  // const { profile } = result
  // const { data } = await imageAPI.createPreSignedUrl({
  //   path: 'dog',
  //   filename: 'test1.png',
  // })
  // const { body } = data
  // const { url, fields } = body
  // const tt = await imageAPI.uploadImage({ url, fields, file: profile[0] })
  // console.log(tt)
  // const { data } = await imageAPI.getImagePresignedUrl('test.png')
  // const data2 = await axios.get(`${data.body}`)
  return <div></div>
}
